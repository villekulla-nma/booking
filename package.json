{
  "name": "villekulla-reservations",
  "version": "0.1.0",
  "private": true,
  "author": "Emanuel Kluge (https://emanuel-kluge.de/)",
  "license": "MIT",
  "scripts": {
    "postinstall": "lerna bootstrap --ignore-prepublish --no-ci",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev": "lerna run dev --parallel --no-bail",
    "prebuild": "rm -rf ./dist",
    "__postbuild_comment__": "Whyever this file is put there by ncc anyways...",
    "postbuild": "rm ./dist/api/package.json",
    "build": "lerna run build --parallel"
  },
  "devDependencies": {
    "lerna": "4.0.0",
    "npm-run-all": "4.1.5",
    "prettier": "2.2.1",
    "typescript": "3.4.3"
  }
}
